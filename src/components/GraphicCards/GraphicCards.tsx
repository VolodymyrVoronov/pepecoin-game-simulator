import { FC } from "react";

import { GraphicCardsContainer } from "./GraphicCards.styled";

const GraphicCards: FC<{}> = (): JSX.Element => {
  return <GraphicCardsContainer>GraphicCards</GraphicCardsContainer>;
};

export default GraphicCards;
